package org.earthsworth.wmatcher.core.model;

/** User-facing classification of a paired class in the comparison tree. */
public enum ClassClassification {
    AUTO,
    FORCE_CHANGED,
    FORCE_UNCHANGED
}
